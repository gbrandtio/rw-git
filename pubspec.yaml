name: rw_git
description: A git wrapper that facilitates the execution of various useful git commands.
version: 1.0.3
repository: https://github.com/gbrandtio/rw-git

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.25.9
  frontend_server_client: ^4.0.0
